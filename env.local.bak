/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: process.env.NEXT_PUBLIC_SITE_URL || 'https://papoomart.vercel.app/',
  generateRobotsTxt: true,
  // Opciones opcionales:
  sitemapSize: 5000,
  exclude: ['/reclamos/privado', '/admin'],
  robotsTxtOptions: {
    additionalSitemaps: [
      // puedes añadir aquí sitemaps adicionales si tienes rutas especiales
    ],
  },
};
